# Runtime dependencies are declared in pyproject.toml.
# This file is kept for users who prefer `pip install -r requirements.txt`.
streamlit>=1.32,<2.0

# Dev (tests + linting). Equivalent to `pip install -e .[dev]`.
pytest>=8.0,<10.0
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
